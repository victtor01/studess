import { Routes } from '@angular/router';
import { DashboardComponent } from './home/dashboard.component';

export const PRIVATE_ROUTES: Routes = [{ path: 'dashboard', component: DashboardComponent }];
